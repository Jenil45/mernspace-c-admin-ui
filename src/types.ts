export type Credentials = {
    email: string;
    password: string;
}

export type User = {
    id: number;
    firstName: string;
    lastName: string;
    role: string;
    email: string;
    createdAt: string;
}